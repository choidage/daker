# ADR-003: ChromaDB를 벡터DB로 선택했다. 이유는 Python 통합이 쉽고 설치가 간편하기 때문이다. 대안으로 LanceDB를 고려했으나 Chrom

> **상태:** 제안
> **작성일:** 2026.02.13
> **추출 방법:** Decision Extractor Agent (신뢰도: 65%)

---

## 컨텍스트

ChromaDB를 벡터DB로 선택했다. 이유는 Python 통합이 쉽고 설치가 간편하기 때문이다. 대안으로 LanceDB를 고려했으나 ChromaDB를 채택했다.

## 고려한 대안

- N/A

## 결정

ChromaDB를 벡터DB로 선택했다. 이유는 Python 통합이 쉽고 설치가 간편하기 때문이다. 대안으로 LanceDB를 고려했으나 ChromaDB를 채택했다.

## 근거

는 Python 통합이 쉽고 설치가 간편하기 때문이다. 대안으로 LanceDB를 고려했으나 ChromaDB를 채택했다.
