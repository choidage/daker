# 질문4: 옵션 B(안정화) + M5(Scale-out) + 권한 고도화 + Docker Compose + UI 개선 — 전체 대화 기록

> **팀 바이브제왕** | **AI: 제왕** | **일시: 2026.02.20**  
> 이 문서는 질문3 이후의 대화를 기록합니다. 옵션 B(결과물 안정화), 옵션 A(M5: Scale-out 멀티 프로젝트), 프로젝트 간 팀원 권한 고도화, 옵션 C(Docker Compose 통합 배포), 피드백 루프 버그 수정, 대시보드 UI 변경(사이드바 → 탭 바) 과정을 포함합니다.

---

## 대화 1: 옵션 B — 결과물 안정화

**사용자:** 옵션B를 진행해줘.

**제왕:** 옵션 B(현재 결과물 안정화)를 진행했습니다.

### 수행 내역:
1. **E2E 테스트 추가**: FastAPI의 모든 API 엔드포인트에 대한 포괄적 E2E 테스트 36개를 11개 클래스로 구성하여 `tests/test_dashboard_api.py`에 추가
2. **인증 시스템 Next.js 마이그레이션**: `useAuth` 훅, 로그인 페이지(`/login`), 관리자 페이지(`/admin`), AppShell 라우트 보호, Sidebar 조건부 렌더링 구현
3. **Gate 4 오탐(SEC-006) 수정**: `review_agent.py`의 SEC-006 정규식을 lookahead assertion으로 수정하여 false positive 제거
4. **Alert 상태 영속화**: `alert_system.py`에 `_save_to_disk()`, `_load_from_disk()` 구현. JSON 파일로 `.meta` 디렉터리에 저장
5. **매직 넘버 추출**: `review_agent.py`에서 500, 5, 20을 `MAX_FILE_LINES`, `MAX_NESTING_DEPTH`, `MAX_IMPORT_COUNT` 상수로 추출
6. **테스트 수정**: `test_types.py`의 cosine distance 기반 `relevance_score` 기대값 보정

### 변경 파일:
- `src/layer3_agents/review_agent.py` — SEC-006 정규식 수정 + 매직 넘버 추출
- `src/layer5_dashboard/alert_system.py` — 영속화 추가
- `dashboard/src/hooks/useAuth.ts` — 인증 훅 (신규)
- `dashboard/src/app/login/page.tsx` — 로그인 페이지 (신규)
- `dashboard/src/app/login/layout.tsx` — 로그인 레이아웃 (신규)
- `dashboard/src/app/admin/page.tsx` — 관리자 페이지 (신규)
- `dashboard/src/app/AppShell.tsx` — 인증 컨텍스트 + 라우트 보호
- `dashboard/src/components/Sidebar.tsx` — 관리자 링크 조건부 표시
- `dashboard/src/lib/api.ts` — 인증 API 타입 + 메서드 추가
- `dashboard/src/lib/i18n.ts` — 인증 관련 번역 키 추가
- `tests/test_dashboard_api.py` — 36개 E2E 테스트
- `tests/test_types.py` — relevance_score 기대값 수정

---

## 대화 2: 옵션 A — M5: Scale-out (멀티 프로젝트 지원)

**사용자:** 옵션A를 진행해줘.. 제왕

**제왕:** M5: Scale-out(멀티 프로젝트 지원, 팀 간 대시보드 통합)을 진행했습니다.

### 수행 내역:
1. **Project Registry 모듈**: `project_registry.py` — 프로젝트 등록/조회/비활성화/목록, JSON 파일 영속화
2. **Project Context Manager**: `project_context.py` — 프로젝트별 격리된 서비스 인스턴스(Metrics, Alerts, WorkZone 등) 관리, lazy-init
3. **프로젝트별 Metrics 격리 + Cross-project 집계 API**: `/api/projects/aggregate` 엔드포인트
4. **FastAPI 라우터 프로젝트 스코프 적용**: 11개 새 멀티 프로젝트 API 엔드포인트 추가
5. **Next.js 프로젝트 스위처 + Multi-project 개요 페이지**: `/projects` 페이지, Sidebar에 프로젝트 링크 추가
6. **E2E 테스트**: 13개 멀티 프로젝트 API 테스트 추가

### 변경 파일:
- `src/layer5_dashboard/project_registry.py` (신규)
- `src/layer5_dashboard/project_context.py` (신규)
- `src/layer5_dashboard/app.py` — 11개 멀티 프로젝트 API
- `dashboard/src/app/projects/page.tsx` (신규)
- `dashboard/src/components/Sidebar.tsx` — projects 링크 추가
- `dashboard/src/lib/api.ts` — ProjectInfo, AggregateSummary 타입 + API
- `dashboard/src/lib/i18n.ts` — 프로젝트 관련 번역 키
- `tests/test_dashboard_api.py` — TestMultiProjectAPI 13개 테스트

---

## 대화 3: 테스트 데이터 오염 버그 수정

**사용자:** 비활성화 된 것은 왜 그랗지?

**제왕:** `projects.json`에 E2E 테스트 데이터가 잔류하여 비활성화 상태 프로젝트가 보이는 것으로 확인. 테스트 격리를 위해 `conftest.py`에 `isolated_project_registry` fixture를 추가하고, TestMultiProjectAPI에서 사용하도록 수정했습니다.

### 변경 파일:
- `tests/conftest.py` — `isolated_project_registry` fixture 추가
- `tests/test_dashboard_api.py` — fixture 적용

---

## 대화 4: 프로젝트 간 팀원 권한 관리 고도화

**사용자:** 우선 프로젝트간 팀원권한 권리 고도화를 진행 해 줘.

**제왕:** 프로젝트별 멤버 역할(Owner/Maintainer/Developer/Viewer) 기반 권한 관리 시스템을 구현했습니다.

### 수행 내역:
1. **ProjectRole Enum**: `owner`, `maintainer`, `developer`, `viewer` 4단계
2. **PROJECT_ROLE_PERMISSIONS**: 역할별 허용 권한 맵 (project:manage, member:add, gate:run 등)
3. **ProjectMember Dataclass**: username, project_role, joined_at, `has_permission()`, `to_dict()`
4. **ProjectInfo 리팩터링**: `team: list[str]` → `members: dict[str, ProjectMember]`
5. **ProjectRegistry 확장**: `add_member`, `remove_member`, `change_member_role`, `transfer_ownership`, `check_project_permission` 등 7개 메서드
6. **영속화 업데이트**: `_save_to_disk`/`_load_from_disk`에 ProjectMember 직렬화/역직렬화 + 하위 호환성
7. **auth.py 확장**: `resolve_project_permission()` — 글로벌 역할 + 프로젝트 역할 통합 권한 확인
8. **FastAPI 6개 새 엔드포인트**: 멤버 목록/추가/제거/역할변경/소유권이전/권한확인
9. **프론트엔드 UI**: 프로젝트 카드 확장 패널에서 멤버 관리 (추가/제거/역할변경), 역할별 아이콘/색상
10. **i18n**: 멤버 관련 16개 번역 키 추가 (ko/en)
11. **E2E 테스트**: 12개 멤버 관리 API 테스트 추가

### 변경 파일:
- `src/layer5_dashboard/project_registry.py` — ProjectRole, ProjectMember, 권한 맵, 멤버 관리 메서드
- `src/layer5_dashboard/auth.py` — `resolve_project_permission()`
- `src/layer5_dashboard/app.py` — 6개 멤버 관리 API
- `dashboard/src/lib/api.ts` — ProjectMember 타입 + 5개 API 메서드
- `dashboard/src/lib/i18n.ts` — 16개 번역 키
- `dashboard/src/app/projects/page.tsx` — 멤버 관리 UI 패널
- `tests/test_dashboard_api.py` — TestProjectMemberAPI 12개 테스트

---

## 대화 5: 옵션 C — Docker Compose 통합 배포 구성

**사용자:** 옵션 C를 진행 해 줘.

**제왕:** Docker Compose 통합 배포 구성을 완료했습니다.

### 수행 내역:
1. **Dockerfile.backend**: python:3.11-slim 기반, build-essential 포함, `/workspace/vibe-x` 워크디렉토리
2. **dashboard/Dockerfile**: node:22-alpine 3-stage 빌드 (deps → builder → runner), Next.js standalone 출력
3. **docker-compose.yml**: backend + frontend 2-서비스, vibex-net 브릿지 네트워크
4. **네임드 볼륨 3개**: vibex-data(.meta), vibex-state(.state), vibex-chromadb(.chromadb)
5. **next.config.ts 수정**: `API_BACKEND_URL` 환경변수 기반 동적 rewrite, `output: 'standalone'`
6. **Health check**: 백엔드 urllib.request, 프론트엔드 wget --spider
7. **depends_on**: frontend → backend (condition: service_healthy)
8. **.dockerignore 2개**: 루트 + dashboard

### 검증 결과:
- `docker compose build backend` — 성공 (약 13분)
- `docker compose build frontend` — 성공 (약 2분)
- `docker compose up -d` — backend Healthy, frontend Ready in 83ms
- `curl localhost:8000/api/dashboard` — 200 OK
- `curl localhost:3000/` — 200 OK

### 변경 파일:
- `Dockerfile.backend` (신규)
- `dashboard/Dockerfile` (신규)
- `docker-compose.yml` (신규)
- `.dockerignore` (신규)
- `dashboard/.dockerignore` (신규)
- `dashboard/next.config.ts` — standalone + API_BACKEND_URL

---

## 대화 6: Docker 실행 + 빌드 검증

**사용자:** DOCKER 를 실행했어.

**제왕:** Docker Desktop 실행을 확인 후, 백엔드/프론트엔드 이미지를 빌드하고 전체 스택을 기동했습니다.
- vibex-backend: Healthy (8000번 포트)
- vibex-frontend: Healthy (3000번 포트)
- API 및 페이지 응답 모두 정상 확인

---

## 대화 7: 피드백 루프 에러 수정

**사용자:** 피드백루프를 확인 해 줘. 에러가 발생하는것 같아.

**제왕:** 3가지 문제를 발견하고 수정했습니다.

### 발견된 에러 3건:

| 문제 | 원인 | 수정 |
|------|------|------|
| 프론트엔드 피드백 페이지 크래시 | `analyze_failure_patterns()`가 빈 이력 시 `total_failures`, `total_runs`, `failure_rate`, `top_messages` 필드를 누락한 불완전 JSON 반환 → `undefined.length` 에러 | `feedback_loop.py` — 빈 이력 시에도 전체 필드 포함한 완전한 구조 반환 |
| 프론트엔드 방어 코드 부재 | API 응답에 필드 누락 시 대비 없음 | `feedback/page.tsx` — `??` nullish coalescing으로 모든 필드 기본값 처리 |
| Docker 프론트엔드 unhealthy | Alpine의 wget이 localhost를 IPv6(::1)로 해석, Node.js는 IPv4만 리스닝 | `dashboard/Dockerfile` healthcheck URL을 `http://127.0.0.1:3000/`으로 변경 |

### 변경 파일:
- `src/layer5_dashboard/feedback_loop.py` — 빈 이력 시 전체 응답 구조 반환
- `dashboard/src/app/feedback/page.tsx` — nullish coalescing 방어 처리
- `dashboard/Dockerfile` — healthcheck IPv6 수정

### 검증 결과:
- 재빌드 후 vibex-backend: healthy, vibex-frontend: healthy
- `localhost:8000/api/feedback` — 200 OK, 데이터 정상
- `localhost:3000/api/feedback` — 200 OK, 빈 상태에서도 안전한 응답

---

## 대화 8: 로컬 서버 복원

**사용자:** 대시보드를 이전처럼 수정 해 줄 수 있겠어?

**제왕:** (처음에는 Docker 중지 + 로컬 서버 복원으로 해석)  
Docker 컨테이너를 중지하고 로컬 uvicorn + Next.js dev 서버를 재시작했습니다.
- 백엔드(`localhost:8000`) — 기존 축적 데이터 포함 정상 응답
- 프론트엔드(`localhost:3000`) — Next.js 개발 서버 정상 가동

---

## 대화 9: 대시보드 UI 변경 — 사이드바 → 탭 바

**사용자:** 내 말 뜻은 현재 대시보드에 사이드바가 보이는데 이전에는 사이드바가 없는 형태로 작업의 진행 흐름도 표시가 되었던 그런 형태를 말하는거야.

**제왕:** 이전 대시보드(`dashboard.html`)의 스타일을 확인하고, 사이드바를 제거하여 상단 수평 탭 바 형태로 변경했습니다.

### 변경 내용:
- **Sidebar 제거**: 좌측 고정 사이드바(w-56) 완전 제거
- **TabBar 신규 생성**: 상단 수평 탭 네비게이션 (border-bottom 활성 표시)
- **Header 통합**: VIBE-X 로고를 헤더에 이동
- **레이아웃 변경**: `flex` (수평) → `flex-col` (수직)으로 전환, `ml-56` 제거

### 레이아웃 구조:
```
┌──────────────────────────────────────────────┐
│  [VX] VIBE-X  Dashboard    🟢Live  KO  user  │  ← 헤더
├──────────────────────────────────────────────┤
│ Overview | Gates | Pipeline | Search | ...   │  ← 탭 바
├──────────────────────────────────────────────┤
│                                              │
│              콘텐츠 영역 (전체 너비)            │
│                                              │
└──────────────────────────────────────────────┘
```

### 변경 파일:
- `dashboard/src/components/TabBar.tsx` (신규) — 수평 탭 네비게이션
- `dashboard/src/app/AppShell.tsx` — Sidebar → TabBar 교체, flex-col 레이아웃
- `dashboard/src/components/Header.tsx` — VIBE-X 로고 통합

---

## 대화 10: 대화 저장 요청

**사용자:** 질문3 이후의 대화내용을 질문4로 저장해줘.

**제왕:** 본 문서(`질문4.md`)로 저장 완료.

---

## 누적 프로젝트 현황 요약

### 완료된 마일스톤:
- **M1: Foundation** — 프로젝트 정의, 아키텍처 맵, 코딩 규칙, ADR, PACT-D
- **M2: RAG Engine** — ChromaDB, 코드 청커/인덱서/시맨틱 검색
- **M3: Agent Chain** — 6-Gate 파이프라인, Work Zone, Decision Extractor
- **M4: Platform** — 대시보드 (FastAPI + Next.js), Alert 시스템, 온보딩 Q&A, IDE 플러그인
- **M5: Scale-out** — 멀티 프로젝트 등록/관리/전환, Cross-project 집계
- **옵션 B: 안정화** — E2E 테스트 36개, 인증 마이그레이션, Gate 4 오탐 수정, Alert 영속화
- **권한 고도화** — ProjectRole 4단계, ProjectMember, 프로젝트별 RBAC, 6개 API + 12개 테스트
- **옵션 C: Docker Compose** — 백엔드/프론트엔드 컨테이너화, 네임드 볼륨, 통합 배포

### 현재 테스트 현황:
- **총 테스트**: 약 160개+ (E2E + 유닛 테스트)
- **전체 통과 상태**

### 주요 기술 스택:
- **백엔드**: Python 3.11, FastAPI, ChromaDB, sentence-transformers, JWT, WebSocket
- **프론트엔드**: Next.js 16, TypeScript, TailwindCSS v4, react-chartjs-2, lucide-react
- **배포**: Docker Compose (python:3.11-slim + node:22-alpine)
- **IDE**: cursor-vibex VSCode Extension

### 다음 가능한 작업:
- 옵션 D: VSIX 배포 패키징 + Marketplace 준비
- 옵션 E: CI/CD 파이프라인 (GitHub Actions)
- 옵션 F: 프로덕션 최적화 (torch CPU-only, 이미지 경량화)
